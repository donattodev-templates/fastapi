# TODO: Implements the adapter so the repositories can execute lambda functions on AWS
# Use a generic approach for almost everything as possible to you can pass for example the region, resource and other via
# method parameter on the repository
